TODO
====

save & load options:
	- using serialization
		* groovy built-in
		* XStream (XML)
	- save nothing, but (re-)analyze the battle
	  pro: makes the AI suitable for jump-in-AI if a human player leaves the battle

discuss the AIs basic architecture:
	eg, should we use an agent based approach?

