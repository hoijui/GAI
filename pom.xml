<project xmlns="http://maven.apache.org/POM/4.0.0"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>gai</groupId>
	<artifactId>GAI</artifactId>
	<packaging>jar</packaging>
	<version>0.1-SNAPSHOT</version>
	<name>GAI</name>
	<url>http://springrts.com/wiki/AI:GAI</url>

	<build>
		<plugins>
			<!--
				Set source compatibility to 1.5, so Annotations are supported.
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>1.5</source>
					<target>1.5</target>
				</configuration>
			</plugin>

			<!--
				Modifies the jars manifest file,
				eg. adds the Main-Class attribute
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<configuration>
					<archive>
						<manifest>
							<addClasspath>true</addClasspath>
							<classpathPrefix>dependency</classpathPrefix>
							<packageName>gai</packageName>
							<mainClass>gai.AIFactory</mainClass>
						</manifest>
					</archive>
				</configuration>
			</plugin>

			<!--
				Copies the dependencies to the target/dependency dir
				during the package phase
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<scm>
		<connection>scm:git:git://github.com/hoijui/GAI.git</connection>
		<url>http://github.com/hoijui/GAI</url>
		<developerConnection>scm:git:git@github.com:hoijui/GAI.git</developerConnection>	
	</scm>

	<dependencies>
		<!--
			For finding Maven dependencies, go to:
			http://www.mvnrepository.com/search.html
		-->
		<dependency>
			<groupId>com.springrts</groupId>
			<artifactId>JavaAIInterface</artifactId>
			<version>0.1</version>
			<scope>system</scope>
			<systemPath>${basedir}/../../../../../builds/cmake/gai/linux32/debug/AI/Interfaces/Java/AIInterface.jar</systemPath>
		</dependency>
		<dependency>
			<groupId>vecmath</groupId>
			<artifactId>vecmath</artifactId>
			<version>0.1</version>
			<scope>system</scope>
			<systemPath>${basedir}/../../Interfaces/Java/data/jlib/vecmath.jar</systemPath>
		</dependency>
		<dependency>
			<groupId>jna</groupId>
			<artifactId>jna</artifactId>
			<version>0.1</version>
			<scope>system</scope>
			<systemPath>${basedir}/../../Interfaces/Java/data/jlib/jna/jna.jar</systemPath>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-beans</artifactId>
			<version>2.5.6</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-support</artifactId>
			<version>2.0.8</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
			<version>2.5.6</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-aop</artifactId>
			<version>2.5.6</version>
			<scope>compile</scope>
		</dependency>

		<dependency>
			<groupId>org.beanshell</groupId>
			<artifactId>bsh</artifactId>
			<version>2.0b4</version>
			<!--<version>1.3.0</version>-->
			<scope>runtime</scope>
		</dependency>
		<!--
			As JRuby 1.1.0+ is incompatible with scripting facilities
			in spring 2.5.6, we have to use an old version of jruby,
			and specify its dependencies manually.
			JRuby 1.1 support is scheduled for Spring 3.0;
			Spring 2.5.x keeps requiring JRuby 0.9 or 1.0.
		-->
		<dependency>
			<groupId>org.jruby</groupId>
			<artifactId>jruby</artifactId>
			<!--<version>1.2.0</version>
			<version>1.3.0RC1</version>-->
			<version>1.0.3</version>
			<!--
				Note: The pom of this version is invalid, but it will work,
				cause we defined the dependencies manually.
				We need to use this version, as it is the last one
				compatible with spring versions < 3.
			-->
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<!-- JRuby 1.0.3 dependency -->
			<groupId>backport-util-concurrent</groupId>
			<artifactId>backport-util-concurrent</artifactId>
			<version>3.1</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<!-- JRuby 1.0.3 dependency -->
			<groupId>asm</groupId>
			<artifactId>asm-all</artifactId>
			<version>2.1</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.codehaus.groovy</groupId>
			<artifactId>groovy-all</artifactId>
			<version>1.6.2</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.7</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

</project>
